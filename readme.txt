note that, before runing python3 main.py, rename the files to makesure they have 'zone' in the file name

For simulator, that they don't have zone. There will be only four files.
To run this, separate four files into a new folder and put that folder into the main.py
